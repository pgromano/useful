from setuptools import setup, find_packages

version = "0.0.0"
setup(
    name="useful",
    author="Pablo Romano",
    description="Useful functions for Python",
    version=version,
    packages=find_packages(),
    zip_safe=False,
)
